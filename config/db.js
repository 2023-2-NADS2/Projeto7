const dbConfig = {
    host: 'localhost', // Colocar 'localhost'
    user: 'root', // Seu úsuario no mysql utilizado no db
    password: '', // Senha para o db
    database: 'drawedu', // Nome do db
  };

  module.exports = dbConfig;